[tool.poetry]
name = "pharmia-backend"
version = "0.1.0"
description = "Backend adaptado do pharm-mvp para pharmIA"
authors = ["Seu Nome <seu@email.com>"]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.110.0"
uvicorn = "^0.29.0"
pandas = "^2.2.0"
scikit-learn = "^1.4.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
